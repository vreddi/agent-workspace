export type GridPosition = {
  x: number;
  y: number;
};

export type GridConfig = {
  width: number;
  height: number;
  cellSize: number;
};

export type WorldPoint = {
  x: number;
  y: number;
};
