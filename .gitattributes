# Generated by the TanStack Start / Router Vite plugin on every dev & build.
# It is committed on purpose (it supplies app-wide route type augmentations),
# but it is generated code: collapse it in GitHub diffs and exclude it from
# language stats so regeneration churn doesn't add review noise.
apps/web/src/routeTree.gen.ts linguist-generated=true
