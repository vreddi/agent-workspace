# Copy to apps/storybook/.env.local (never commit).
#   cp apps/storybook/.env.local.example apps/storybook/.env.local
#
# Used by `nx run @org/storybook:chromatic` (cwd is apps/storybook).

CHROMATIC_PROJECT_TOKEN=chpt_
